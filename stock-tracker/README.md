# Stock Tracker — Persistent Watchlist + Live Prices

## Quick Start
```bash
cd ~/stock-tracker && ./deploy.sh
```

## Deploy Workflow
1. Make changes → `git add . && git commit -m "..."`  
2. On VPS: `./deploy.sh` (pulls latest, rebuilds container)
